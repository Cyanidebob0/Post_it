<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Form</title>
  </head>
  <body class="h-full">
    <div class="bg-zinc-900 w-full h-screen flex items-center justify-center">
      <form
        action="/create"
        method="post"
        class="bg-white p-8 rounded-lg shadow-md w-80 space-y-4"
      >
        <h2 class="text-2xl font-bold text-center text-zinc-800">Register</h2>

        <div>
          <label class="block text-sm font-medium text-zinc-700"
            >Username</label
          >
          <input
            type="text"
            name="username"
            class="mt-1 w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-zinc-500"
            required
          />
        </div>

        <div>
          <label class="block text-sm font-medium text-zinc-700">Email</label>
          <input
            type="email"
            name="email"
            class="mt-1 w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-zinc-500"
            required
          />
        </div>

        <div>
          <label class="block text-sm font-medium text-zinc-700"
            >Password</label
          >
          <input
            type="password"
            name="password"
            class="mt-1 w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-zinc-500"
            required
          />
        </div>

        <div>
          <label class="block text-sm font-medium text-zinc-700">Age</label>
          <input
            type="number"
            name="age"
            class="mt-1 w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-zinc-500"
            required
          />
        </div>

        <button
          type="submit"
          class="w-full bg-zinc-800 text-white py-2 rounded-md hover:bg-zinc-700 transition duration-200"
        >
          Submit
        </button>
      </form>
    </div>
  </body>
</html>
